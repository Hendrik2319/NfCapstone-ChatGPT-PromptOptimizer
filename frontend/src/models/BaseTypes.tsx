export const SHOW_RENDERING_HINTS: boolean = true;
export const DEBUG: boolean = true;

export type Prompt = {
    prompt: string
}
export type Answer = {
    answer: string
}

export type ApiState = {
    enabled: boolean
}
