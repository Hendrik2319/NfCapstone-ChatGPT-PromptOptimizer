export type Prompt = {
    prompt: string
}
export type Answer = {
    answer: string
}

export type ApiState = {
    enabled: boolean
}

export type UserInfos = {
    isAuthenticated : boolean
    id          : string
    login       : string
    name        : string
    location    : string
    url         : string
    avatar_url  : string
}
